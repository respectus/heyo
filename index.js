var geoHash = function(longitude, lattitude, precision) {
    var hash;
    // make hash
    return hash;
};

var imGettingPlaces = function(hash, precision, radius) {
    var places = [];
    // fill array
    return places;
};

module.exports = {
    geoHash: geoHash
    , getPlaces: imGettingPlaces
};
